The Hilbert transform of the input vector is computed and stored in the output vector. The
transform is computed from a forward real-to-complex FFT, a complex multiplication by -j,
followed by an inverse complex-to-real FFT.
